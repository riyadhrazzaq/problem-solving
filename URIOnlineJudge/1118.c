#include<stdio.h>
int main()
{
    int i,j,x;
    float n;
    while(scanf("%f",&n)&&x!-2){c=0;
            while(c<3){ sum=0;
        if(n<0||n>10){printf("nota invalida\n");}
        else{
            c++; sum=sum+n;
        }
    }
    printf("media = %.2f\n",sum/2);
    scanf("%f",&x);
    if(x<1&&x>2)
    }

}
