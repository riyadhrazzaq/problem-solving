#include<stdio.h>
#include<math.h>
int main()
{
    long long int n,;
    double x,y;
    while(scanf("%d",&n)&&n!=0)
    {
        x=sqrt(n);
        y=cbrt(n);
        if()
        if()

    }
    return 0;
}

